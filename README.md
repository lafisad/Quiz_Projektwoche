# txhcQuizTool v0.1

## authored by txhc / Lasse Saalmann + various AI Agents (mainly OpenCode + Big Pickle)

This quiz tool uses JSON as a format for the quiz questions.
<br>
An example for how the quiz questions are to be parsed can be found in [this file](examples/questions_example.json)
<br>
The config file can be found in [this JSON file](config/config.json) - and the example in [](examples/config_example.json)